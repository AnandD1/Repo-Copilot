# Phase 4 Workflow - Visual Diagram

```
╔══════════════════════════════════════════════════════════════════════════╗
║                    PHASE 4: MULTI-AGENT WORKFLOW                         ║
║                     LangGraph Orchestration                              ║
╚══════════════════════════════════════════════════════════════════════════╝


                                  START
                                    │
                                    │ Initial State:
                                    │ - PR hunks
                                    │ - Repo info
                                    │ - Run ID
                                    ▼
                      ┌─────────────────────────┐
                      │   NODE 1: RETRIEVER     │
                      │                         │
                      │  • Local context        │
                      │  • Similar code (vector)│
                      │  • Conventions/guides   │
                      │  • BGE reranking        │
                      └────────────┬────────────┘
                                   │
                                   │ RetrievalBundle per hunk
                                   │
                                   ▼
                      ┌─────────────────────────┐
                      │   NODE 2: REVIEWER      │
                      │                         │
                      │  • LLM analysis         │
                      │  • Evidence-based       │
                      │  • Structured issues    │
                      │  • JSON output          │
                      └────────────┬────────────┘
                                   │
                                   │ ReviewIssue[]
                                   │
                                   ▼
                      ┌─────────────────────────┐
                      │   NODE 3: PLANNER       │
                      │                         │
                      │  • Group issues         │
                      │  • Create fix tasks     │
                      │  • Effort estimates     │
                      │  • Suggested approach   │
                      └────────────┬────────────┘
                                   │
                                   │ FixTask[]
                                   │
                                   ▼
                      ┌─────────────────────────┐
                      │   NODE 4: GUARDRAIL     │
                      │                         │
                      │  ✓ Schema validation    │
                      │  ✓ Secret scanning      │
                      │  ✓ Prompt injection     │
                      │  ✓ Evidence enforcement │
                      └────────────┬────────────┘
                                   │
                                   │ GuardrailResult
                                   │
                        ┌──────────┴──────────┐
                        │    Passed/Failed    │
                        │  (both go to HITL)  │
                        └──────────┬──────────┘
                                   │
                                   ▼
                      ┌─────────────────────────┐
                      │   NODE 5: HITL GATE     │
                      │                         │
                      │  👤 Human Decision:     │
                      │   1. Approve            │
                      │   2. Edit               │
                      │   3. Reject             │
                      │   4. Summary only       │
                      └────────────┬────────────┘
                                   │
                      ┌────────────┴────────────┐
                      │                         │
                 APPROVE/EDIT             REJECT
              SUMMARY_ONLY                      │
                      │                         │
                      ▼                         ▼
         ┌─────────────────────────┐   ┌────────────────┐
         │  NODE 6: PUBLISHER      │   │  NODE 7:       │
         │                         │   │  PERSISTENCE   │
         │  • Format GitHub        │   │  (reject path) │
         │    comment              │   │                │
         │  • Post review          │   │  • Save state  │
         │  • Slack notification   │   │  • Mark failed │
         │  • Email notification   │   └────────┬───────┘
         └────────────┬────────────┘            │
                      │                         │
                      │ Posted URL              │
                      │                         │
                      ▼                         │
         ┌─────────────────────────┐            │
         │  NODE 7: PERSISTENCE    │            │
         │  (approved path)        │            │
         │                         │            │
         │  • Save full state      │            │
         │  • Generate summary     │            │
         │  • Track all results    │            │
         └────────────┬────────────┘            │
                      │                         │
                      └────────┬────────────────┘
                               │
                               ▼
                              END
                               
                    Final State Persisted:
                    • workflow_runs/{run_id}.json
                    • workflow_runs/{run_id}_summary.md


╔══════════════════════════════════════════════════════════════════════════╗
║                          STATE TRANSITIONS                                ║
╚══════════════════════════════════════════════════════════════════════════╝

WorkflowState Fields Updated at Each Node:

┌────────────────┬──────────────────────────────────────────────────────┐
│ Node           │ State Updates                                        │
├────────────────┼──────────────────────────────────────────────────────┤
│ Retriever      │ retrieval_bundles: Dict[hunk_id, RetrievalBundle]   │
│                │ current_node: "retriever"                            │
├────────────────┼──────────────────────────────────────────────────────┤
│ Reviewer       │ review_issues: List[ReviewIssue]                     │
│                │ current_node: "reviewer"                             │
├────────────────┼──────────────────────────────────────────────────────┤
│ Planner        │ fix_tasks: List[FixTask]                             │
│                │ current_node: "planner"                              │
├────────────────┼──────────────────────────────────────────────────────┤
│ Guardrail      │ guardrail_result: GuardrailResult                    │
│                │ current_node: "guardrail"                            │
├────────────────┼──────────────────────────────────────────────────────┤
│ HITL           │ hitl_decision: HITLDecision                          │
│                │ current_node: "hitl"                                 │
├────────────────┼──────────────────────────────────────────────────────┤
│ Publisher      │ posted_comment_url: str                              │
│                │ notification_sent: bool                              │
│                │ current_node: "publisher"                            │
├────────────────┼──────────────────────────────────────────────────────┤
│ Persistence    │ persisted: bool                                      │
│                │ persistence_path: str                                │
│                │ current_node: "persistence"                          │
└────────────────┴──────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                      CONDITIONAL ROUTING LOGIC                            ║
╚══════════════════════════════════════════════════════════════════════════╝

After Guardrail:
  ┌─────────────────────────────────────────────┐
  │ should_proceed_to_hitl(state)               │
  │   → Always returns "hitl"                   │
  │   → Even if guardrails failed               │
  │   → User can see blocked reasons            │
  └─────────────────────────────────────────────┘

After HITL:
  ┌─────────────────────────────────────────────┐
  │ should_publish(state)                       │
  │                                             │
  │   if action == APPROVE:                     │
  │      → "publish"                            │
  │                                             │
  │   if action == EDIT:                        │
  │      → "publish" (with edits)               │
  │                                             │
  │   if action == POST_SUMMARY_ONLY:           │
  │      → "publish" (summary mode)             │
  │                                             │
  │   if action == REJECT:                      │
  │      → "persistence_reject"                 │
  │                                             │
  │   else:                                     │
  │      → "persistence_reject" (unknown)       │
  └─────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                         DATA FLOW EXAMPLE                                 ║
╚══════════════════════════════════════════════════════════════════════════╝

Input Hunk:
  ┌─────────────────────────────────────────────┐
  │ file: src/app.py                            │
  │ lines: 45-50                                │
  │ added: ["result = unsafe_eval(user_input)"] │
  │ removed: ["result = process(user_input)"]   │
  └─────────────────────────────────────────────┘
                    ↓
Retrieval Bundle:
  ┌─────────────────────────────────────────────┐
  │ local: [similar functions in src/app.py]   │
  │ similar: [eval usage in other files]       │
  │ conventions: [SECURITY: Never use eval()]  │
  └─────────────────────────────────────────────┘
                    ↓
Review Issue:
  ┌─────────────────────────────────────────────┐
  │ severity: BLOCKER                           │
  │ category: SECURITY                          │
  │ file: src/app.py:45                         │
  │ issue: "eval() allows code injection"      │
  │ suggestion: "Use ast.literal_eval()"       │
  │ evidence: [CONVENTION: Security Guide]     │
  └─────────────────────────────────────────────┘
                    ↓
Fix Task:
  ┌─────────────────────────────────────────────┐
  │ title: "Remove unsafe eval usage"          │
  │ why: "Critical security vulnerability"     │
  │ files: [src/app.py]                        │
  │ approach: "Replace with literal_eval"      │
  │ effort: S (small)                          │
  └─────────────────────────────────────────────┘
                    ↓
Guardrail: PASS ✓
                    ↓
HITL Decision: APPROVE
                    ↓
GitHub Comment:
  ┌─────────────────────────────────────────────┐
  │ ## 🤖 Automated Code Review                 │
  │                                             │
  │ ### 🔴 BLOCKER Issues                       │
  │                                             │
  │ **[SECURITY] src/app.py:45**                │
  │ eval() allows code injection               │
  │ 💡 Suggestion: Use ast.literal_eval()       │
  │ 📎 Evidence: [CONVENTION: Security Guide]   │
  │                                             │
  │ ### 🔧 Fix Plan                             │
  │ 1. Remove unsafe eval usage [S]            │
  └─────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                           TECHNOLOGIES USED                               ║
╚══════════════════════════════════════════════════════════════════════════╝

LangGraph  ──→  Workflow orchestration, state management, routing
LangChain  ──→  LLM integration, prompt templates
Pydantic   ──→  Type-safe models, validation
Ollama     ──→  Local LLM (qwen2.5-coder:7b-instruct)
Qdrant     ──→  Vector search for retrieval
Python     ──→  Implementation language


╔══════════════════════════════════════════════════════════════════════════╗
║                        EXECUTION FLOW SUMMARY                             ║
╚══════════════════════════════════════════════════════════════════════════╝

1. Initialize WorkflowState with PR hunks
2. Stream through nodes sequentially
3. Each node updates state and returns dict
4. LangGraph merges updates into state
5. Conditional edges route based on state
6. Final state persisted to disk
7. Results available for inspection

Time per hunk: ~5-10 seconds
Total for 10 hunks: ~1-2 minutes


╔══════════════════════════════════════════════════════════════════════════╗
║                              STATUS: ✅ COMPLETE                          ║
╚══════════════════════════════════════════════════════════════════════════╝
```
